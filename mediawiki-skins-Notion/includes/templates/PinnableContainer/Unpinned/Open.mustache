{{! Opening div of the unpinned (in-dropdown) placement of a pinnable region. Data: string id - the region id, e.g. notion-main-menu; bool is-pinned - present but deliberately unused here. Emits no content; must be closed by PinnableContainer/Close.mustache. }}
<div id="{{id}}-unpinned-container" class="notion-unpinned-container">
